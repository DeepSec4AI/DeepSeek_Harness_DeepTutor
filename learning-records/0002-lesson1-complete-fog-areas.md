# L1 完成：测验全对 + 事件溯源思考题部分命中 + 雾区定位

用户完成第 1 课，3 道检索题全对（step 定义、「模型可见即已记录」不变量、ctx.tools 归属）——地图级概念已入脑。

**思考题（为何只存事件、投影出模型历史）回答命中的点**：日志结构化（字段/时间戳/中间步骤可见）、调试可溯源。**未命中的关键洞见**：事件溯源的核心价值是「单一事实源 → 多个派生视图」——只存一份不可变事实，UI / fork / 恢复 / 回放 / 遥测各自投影，永远不会出现两份可漂移的状态副本；且投影逻辑改进后旧会话可重新计算。讲解时用 TensorBoard 类比（训练循环只追加事件文件，看板进程实时 tail 该文件），贴合其 DL 背景。已在本会话讲解，待后续检验。

**提问暴露的雾区**（全部是阶段 2 Cordis 主题，路线图无需调整）：
- 「特权内核」「注册即副作用」「挂载在旁边」等框架词汇
- profile /「具名」/ patch 分层应用顺序
- 事件「广播」的目的（持久化 vs 实时通知的分离）

**Implications**：L5–7（Cordis）讲解时直接回收这四个问题作引子；本会话已口头解答并配类比（插线板/回执、VSCode 设置分层、TensorBoard tail），学习记录确认已讲、待检验。
